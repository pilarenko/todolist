import React from 'react';
import Logo from '../UI/Logo/Logo';
import './Footer.css';

const Footer = (props) => {
  return (
    <div className='footer'>
      <Logo />
    </div>
  )
}

export default Footer
